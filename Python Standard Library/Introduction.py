"""
* A library that can be use on any machine where you have an application using the specific language.
* Includes built-in functions and built-in imported modules
* A standard is a built-in.
* A external library is something you need to download separately from the Internet to get a special functionally.

"""

print(min(min("Malik", "Mohan", "Albert")))
# select the minimum from the given data ("Albert") than minimum in "Albert" which is "A"

print(min("_Mohan__", "__Mohan"))

list1 = [15, 12, 25, 45, 13]
print(min(list1))

